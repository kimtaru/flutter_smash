import 'package:flutter/material.dart';

class BoardFragment extends StatelessWidget {
  const BoardFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('BoardFragment'));
  }
}
