import 'package:flutter/material.dart';

class ClubFragment extends StatelessWidget {
  const ClubFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ClubFragment'));
  }
}
